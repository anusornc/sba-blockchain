# Copy to .env for local development.
# Never commit real secrets.
JWT_SECRET=replace-with-a-64-character-local-secret
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
TRANSACTION_API_AUTH_REQUIRED=true
DATOMIC_PROFILE=mem
