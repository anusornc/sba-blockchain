# Current Benchmarks

This directory contains benchmark harnesses and evidence workflows that are
eligible for the current paper revision after registration in
`evidence/paper-current/`.

Legacy benchmark paths may keep small compatibility wrappers, but new work
should use the canonical paths under this directory.
